const { description, small_stock, medium_stock, large_stock, price, mrp, category_id, brand_id, subcategory_id } = req.body

    <%if (subcategoryFind[0].category[0].category == (value.category)) {
    %> selected
            <%}%>